import * as anchor from "@coral-xyz/anchor";

module.exports = async function (_provider: anchor.AnchorProvider) {
  // This repository uses explicit bootstrap scripts instead of automatic migration logic.
};
